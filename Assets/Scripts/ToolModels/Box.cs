using UnityEngine;
using System.Collections;

public class Box : MonoBehaviour {


    //There's no model yet for this script, only the HUD!

    public enum Position
    {
        BEHIND = 1,
        SIDE = 0,
        FRONT = 2,
    }

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
